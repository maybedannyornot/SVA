# SVA: SimpleVariantAnnotator
SVA is a simple tool for variant annotation. Based on a sequence (FASTA format) and its annotations (GFF format), it analyzes a list of variants (VCF format), determines the region in which they occur (intergenic/intron/exon), and identifies how they modify the protein sequence.
